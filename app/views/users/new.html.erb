<%= render 'layouts/sidebar_main' %>

<div class="page-header">
    <h2><%= fa_icon 'dot-circle-o' %> <%= t "crud.add.title", model: User.to_s.humanize.constantize.model_name.human %></h2>
</div>

<%= render 'form' %>

<div class="form-links">
  <%= link_to fa_icon('chevron-left') + t('crud.link.back'), users_path, class: 'label label-primary' %>
</div>

